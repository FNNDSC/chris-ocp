apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: queue
  name: queue
spec:
  containers:
  - image: rabbitmq:3
    name: queue
    resources:
      limits:
        cpu: "100m"
        memory: "256Mi"
      requests:
        cpu: "100m"
        memory: "256Mi"
  restartPolicy: OnFailure
status: {}
